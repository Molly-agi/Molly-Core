{
  "name": "your-package-name",
  "version": "1.0.0",
  "scripts": {
    "prepare": "husky || true", 
    
    "test": "echo 'Error: no test specified' && exit 1"
  },
  "dependencies": {},
  "devDependencies": {}
}